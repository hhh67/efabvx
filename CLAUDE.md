# Claude Code 開発プロセス改善ガイド

## 重要なルール

### 最優先原則
1. **表示担保**: コンポーネントが見えなくなることは絶対に禁止
2. **段階的実装**: 複雑な変更は小さなステップに分割
3. **継続的検証**: 各段階で必ず動作確認
4. **文書化**: 全ての判断と変更を記録

## 問題の原因分析

### なぜカードが表示されなくなったのか
1. **透明度制御の問題**: `opacity: isVisible`で`useTransform`を使用したため、初期値が0になってカードが見えない状態になった
2. **表示担保の認識不足**: 「絶対に表示されなくするな」の指示を軽視し、アニメーション優先で実装した
3. **デバッグ不足**: 実装後の動作確認を怠り、カードが見えない状態に気づかなかった
4. **複雑な構造変更**: stickyからabsoluteへの変更で予期しない副作用が発生

### プロセスの問題点
1. **制約条件の優先順位付け不足**: 「表示担保」が最優先制約だったのに、アニメーション実装を優先した
2. **段階的実装の欠如**: 一度に複数の変更を加えて、どの変更が問題を引き起こしたか特定困難
3. **フォールバック戦略なし**: アニメーション実装時に表示が失われた場合の対策を用意していない
4. **文書管理不足**: 変更履歴と学習内容の記録が不十分

## 改善されたプロセス

### 1. 制約条件の明確化と優先順位付け
```markdown
【最優先制約】
- カードの表示を絶対に担保する（opacity: 1 固定）
- ユーザビリティを損なわない

【次優先】
- 機能要件（スナップスクロール等）
- アニメーション効果
```

### 2. 段階的実装プロセス
```markdown
STEP 1: 制約条件を満たす最小実装
STEP 2: 動作確認とテスト
STEP 3: 機能追加（1つずつ）
STEP 4: 各段階での動作確認
STEP 5: 問題発生時は前段階にロールバック
```

### 3. 必須チェックリスト
```markdown
□ 全カードが画面に表示されているか
□ スクロール機能が正常に動作するか
□ 既存の動作が維持されているか
□ エラーが発生していないか
□ パフォーマンスに問題がないか
```

### 4. 表示担保のためのベストプラクティス
```typescript
// ❌ 危険: 透明度が0になる可能性
opacity: useTransform(progress, [0, 1], [0, 1])

// ✅ 安全: 最小値を保証
opacity: useTransform(progress, [0, 1], [0.3, 1])

// ✅ 最も安全: 常に表示
opacity: 1
```

## 今後の指示方法

### 効果的な指示の出し方
1. **制約条件を最初に明記**
   ```
   【必須制約】カードが表示されなくなることは絶対に禁止
   【要件】スナップスクロール効果の実装
   【優先度】制約 > 要件
   ```

2. **段階的実装を指示**
   ```
   STEP1: まず表示を担保した基本実装
   STEP2: 動作確認後、スナップスクロール追加
   各段階で必ず動作確認すること
   ```

3. **検証方法を具体化**
   ```
   実装後以下を確認:
   - 全5枚のカードが見えているか
   - スクロールできるか
   - エラーがないか
   ```

## 次回以降のワークフロー

### 1. 要件受領時
```markdown
1. 制約条件の抽出と優先順位付け
2. 実装方針の策定
3. リスクの洗い出し
4. 段階的実装計画の作成
```

### 2. 実装時
```markdown
1. 制約条件を満たす最小実装
2. 動作確認（チェックリスト使用）
3. 機能追加（1つずつ）
4. 各段階での検証
5. 問題発生時の即座のロールバック
```

### 3. 完了時
```markdown
1. 全チェックリスト項目の確認
2. エッジケースのテスト
3. パフォーマンス確認
4. ユーザビリティ確認
```

## 緊急時対応

### カードが表示されない場合
```typescript
// 即座に適用する緊急修正
style={{ 
  opacity: 1, // 強制表示
  display: 'block', // 表示確保
  visibility: 'visible' // 可視性確保
}}
```

### ロールバック手順
1. 直前の動作していた状態に戻す
2. 動作確認
3. 問題のある変更を特定
4. 安全な方法で再実装

この改善プロセスにより、今後は表示担保を最優先とした安全な開発が可能になります。

## 自動活用のための設定

### .claude/instructions.md
Claude Codeが自動的に参照するべき最重要ルールを記載。会話開始時に「.claude/instructions.mdの内容を適用してください」と指示することで即座に適用可能。

### .claude/context.md  
プロジェクトの現在状況、頻発する問題パターン、学習済み失敗例を記載。コンテキスト理解の迅速化に活用。

### 使用方法
```
「.claude/instructions.mdと.claude/context.mdを読み込んでから作業を開始してください」
```

これにより、毎回の指示で同じルールを繰り返す必要がなくなり、一貫した品質での開発が可能になります。