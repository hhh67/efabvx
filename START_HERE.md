# 🚨 Claude Code 作業開始前必読 🚨

## 自動読み込み指示

**毎回この一文をコピペしてください：**

```
.claude/instructions.mdと.claude/context.mdを読み込んでから作業を開始してください。表示担保を最優先とし、段階的実装を厳守してください。
```

## なぜ必要なのか

Claude Codeは会話をまたいで情報を保持できないため、重要なルールと過去の学習内容を毎回読み込む必要があります。

## 短縮版指示（最小限）

時間がない場合は以下をコピペ：

```
読み込み: .claude/instructions.md + .claude/context.md
ルール: 表示担保最優先 + 段階的実装
```

## 今後の改善予定

Claude Codeの機能改善により、将来的には以下が可能になる可能性があります：
- 自動ファイル読み込み
- プロジェクト設定の記憶
- セッション間の学習内容継承

現時点では上記の指示文をコピペすることが最も効率的な方法です。