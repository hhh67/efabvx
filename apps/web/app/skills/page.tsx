import { Stack, PageContainer } from '@efabvx/ui';

export default function SkillsPage() {
  return (
    <PageContainer>
      <Stack gap={6}>
        <h1>Skills</h1>
        <p>ここにスキル一覧を移行予定 (legacy から段階移植)。</p>
      </Stack>
    </PageContainer>
  );
}
